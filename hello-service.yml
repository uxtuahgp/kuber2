apiVersion: v1
kind: Service
metadata:
  name: hello-world-service
spec:
  selector:
    name: echoserver
  ports:
    - protocol: TCP
      name: echoserver-port
      port: 18080
      targetPort: 8080
